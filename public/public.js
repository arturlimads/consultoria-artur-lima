const form=document.querySelector('#anamForm');if(form)form.addEventListener('submit',async e=>{e.preventDefault();const msg=document.querySelector('#anamMsg');msg.textContent='Enviando...';const data=Object.fromEntries(new FormData(form));const r=await fetch('/api/anamnese',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)});const j=await r.json();msg.className=r.ok?'ok':'danger';msg.textContent=j.message||j.error;if(r.ok)form.reset();});
